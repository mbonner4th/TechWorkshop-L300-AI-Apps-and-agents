name: Update Cora Agent

on:
  push:
    branches:
      - main
    paths:
      - 'src/infra/agents/cora.json'
      - 'src/prompts/ShopperAgentPrompt.txt'
  workflow_dispatch:

jobs:
  deploy-agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2.1.1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Prepare agent definition
        id: definition
        run: |
          jq \
            --arg gpt_deployment "${{ secrets.GPT_DEPLOYMENT }}" \
            --rawfile instructions src/prompts/ShopperAgentPrompt.txt \
            '.definition.model = $gpt_deployment | .definition.instructions = $instructions' \
            src/infra/agents/cora.json > /tmp/agent-definition.json
          echo "definition=$(cat /tmp/agent-definition.json | jq -c .)" >> $GITHUB_OUTPUT

      - name: Deploy agent version to Foundry
        run: |
          cat /tmp/agent-definition.json | az rest \
            --method POST \
            --url "${{ secrets.FOUNDRY_ENDPOINT }}/agents/cora/versions?api-version=2025-11-15-preview" \
            --resource "https://ai.azure.com" \
            --headers "Content-Type=application/json" \
            --body @-
